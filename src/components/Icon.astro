---
type Props = {
  id: string;
  class?: string;
};
const { id, class: className, ...rest } = Astro.props;
---

<svg class={className} {...rest}>
  <use href={`/icons/icons.svg#${id}`}></use>
</svg>
